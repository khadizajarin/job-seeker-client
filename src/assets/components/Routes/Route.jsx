import { Outlet } from "react-router-dom";
// import Home from "../Layout/Home";


const Route = () => {
    return (
        <div>
            <Outlet></Outlet>
        </div>
    );
};

export default Route;